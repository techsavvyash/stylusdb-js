procs:
  raft-node-1:
    shell: "node server.js --port 8081"
  raft-node-2:
    shell: "node server.js --port 8082"
  raft-node-3:
    shell: "node server.js --port 8083"
  raft-node-4:
    shell: "node server.js --port 8084"
  proxy:
    shell: "node proxy.js"
  user-shell:
    cmd: ["zsh"]